# Core Framework
fastapi
uvicorn[standard]

# Database
# Pinned to versions verified working locally + on Cloud Run.
# Unpinned versions pulled a newer incompatible release ("MotorDatabase object is not callable")
# that broke container startup. Keep these pinned together.
pymongo==4.13.2
motor==3.7.1
beanie==1.30.0

# Authentication & Security
python-jose[cryptography]
passlib[bcrypt]
bcrypt==4.0.1  # Pin to compatible version with passlib 1.7.4
python-multipart
email-validator
pydantic[email]>=2.10
pydantic-settings
slowapi
nh3==0.2.14  # Fast HTML sanitization for XSS prevention

# AI Integration
pydantic-ai
youtube-transcript-api

# Email Service (Microsoft Graph API)
msal
requests
anthropic

# Email (using Microsoft Graph API)

# Testing
pytest
pytest-asyncio
httpx

# Development Tools
ruff
black

# Monitoring (removed Sentry - using standard logging)

# Utilities
python-dotenv
python-slugify
python-magic==0.4.27
pytz
stripe

# Report Generation
reportlab

# Cloud Storage
google-cloud-storage==2.10.0